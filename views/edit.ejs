<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Edit</title>
</head>
<body>
    <div class="w-full min-h-screen text-white p-5 sm:p-10" style="background-color: lightblue;">
        <div class="flex justify-start w-full mb-5">
            <a class="bg-red-500 rounded-md px-3 py-2" href="/logout">Logout</a>
        </div>

        <h5 class="mb-5 text-xl">You can edit your post.</h5>

        <form action="/update/<%=post.id%>" method="post">
            <!-- Make textarea responsive without changing the UI -->
            <textarea 
                style="font-weight: 500; font-family:'Courier New', Courier, monospace;" 
                class="resize-none bg-transparent text-xl text-black border-2 border-zinc-200 w-full sm:w-1/2 p-3 outline-none" 
                placeholder="What's on your mind?" name="content" id="">
                <%=post.content%>
            </textarea>

            <!-- Submit button remains responsive without UI changes -->
            <input class="py-2 px-3 bg-yellow-600 block rounded-md mt-2 w-full sm:w-40" value="Update Post" type="submit">
        </form>
    </div>
</body>
</html>
