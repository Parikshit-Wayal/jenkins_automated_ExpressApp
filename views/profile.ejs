<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>@<%= user.username %></title>
</head>
<body>
    <div class="w-full min-h-screen text-white p-5 sm:p-10" style="background-color: lightblue;">
        <div class="flex justify-end w-full">
            <a class="bg-red-500 rounded-md px-3 py-2" href="/logout">Logout</a>
        </div>
        <br>
        <h3 class="text-black font-bold mb-5 mt-5 text-2xl sm:text-3xl" style="font-family: cursive, lobster;">
            <span class="font-light bg-zinc-400 px-1 rounded" style="font-family: lobster, cursive;">Hello Dear</span>, <%= user.username %> 👋
        </h3>
        <h5 class="mb-5 text-lg sm:text-xl">You can create a new post.</h5>
        <form action="/post" method="post" class="w-full sm:w-1/2 lg:w-1/3">
            <textarea class="resize-none bg-transparent text-lg text-black border-2 border-zinc-200 w-full p-3 outline-none rounded-md" style="font-weight: 500; font-family: 'Courier New', Courier, monospace;" name="content" placeholder="What's on your mind?"></textarea>
            <input class="py-2 px-3 bg-blue-600 block rounded-md mt-2 w-full sm:w-40" value="Create new post" type="submit">
        </form>
        <div class="post mt-10 sm:mt-20">
            <h3 class="text-zinc-800 font-bold text-xl sm:text-2xl" style="font-family: 'Courier New', Courier, monospace;">Your posts..</h3>
            <div class="postcontainer mt-5 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5">
                <% user.posts.reverse().forEach(function(post) { %>
                    <div class="post bg-antiquewhite m-5 p-4 border-2 rounded-md border-zinc-800 " style="background-color: antiquewhite;">
                        <h4 class="text-blue-700 text-lg sm:text-2xl font-semibold">@<%= user.username %></h4>
                        <p class="text-black mt-3 ml-2 sm:ml-5 text-base sm:text-lg" style="font-weight: 500;"><%= post.content %></p>
                        <small class="text-black mt-3 ml-2 sm:ml-5 block"><%= post.likes.length %> Likes</small>
                        <div class="btns flex gap-3 ml-2 sm:ml-5 mt-3">
                            <a class="text-blue-500 text-base sm:text-lg" href="/like/<%= post.id %>">
                                <%= post.likes.indexOf(user._id) === -1 ? "like" : "unlike" %>
                            </a>
                            <a class="text-zinc-500 text-base sm:text-lg" href="/edit/<%= post.id %>">edit</a>
                            <a class="text-red-500 text-base sm:text-lg" href="/delete/<%= post.id %>">delete</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>
